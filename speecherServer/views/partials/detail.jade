.container
  .write-box
    .input-group
      input.form-control(type='text', placeholder="{{vm.scriptTitle}}", ng-model="title", readonly)
      a.input-group-addon.detail-btn-history(href='/#/') 테스트 기록보기
      a.input-group-addon.detail-btn-test(href='/#/') 테스트하기
  .write-box(ng-show="vm.wordsDummy")
    h4 자주 틀리는 단어
    ul.nav.nav-pills(role='tablist')
      li(role='presentation', ng-repeat='item in vm.wordsDummy')
        a.btn-word(href='#')
          | {{item.word}}
          span.badge {{item.count}}
  .write-box
    textarea.form-control.noresize.script-content(placeholder="{{vm.scriptContent}}", ng-model="content", readonly)
    button.btn-hidden
    button.btn.btn-test(ng-click='') 삭제
    button.btn.btn-save(ng-click='') 수정


